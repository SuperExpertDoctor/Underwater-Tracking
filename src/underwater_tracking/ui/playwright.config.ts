// The default browser suite must exercise the same real backend contract as
// the explicit live acceptance suite. There is no frontend-owned frame source.
export { default } from "./playwright.live.config";
