# UUV Initialization and Local Perception Acceptance

Date: 2026-08-22
Branch: `feature/uuv-initialization-local-perception`
Seed: `20260820` for deterministic backend acceptance; `42` for the default entrypoint smoke check.

## Backend acceptance

- The real `SimulationEngine` and `_AgentLoop` timeline passed at 430 steps. It verified the four-carrier topology, 12 onboard UUVs, deployment at 120 s, waterborne visibility only after deployment, distinct scan routes, recovery, moving-carrier return, local target evidence, hysteresis, and periodic-memory cursor behavior.
- The opt-in 8-hour test passed:
  `UNDERWATER_TRACKING_RUN_8H=1 PYTHONPATH=src:. python -m pytest tests/integration/test_uuv_only_8h_replay_acceptance.py -m long_running -q --tb=short`
  Result: `1 passed in 2099.73s (0:34:59)`.
- The long test replays the canonical `operational_frames.jsonl` generated by the production publisher. It validated all 5,760 frames, monotonic time, bounded headings, no USV fields, 48 idempotent periodic summaries, plan evolution, and UUV deployment/recovery events.
- Full backend suite:
  `PYTHONPATH=src python -m pytest -q --tb=short`
  Result: `1199 passed, 68 skipped, 2 warnings in 125.40s`.

## Frontend acceptance

- Vitest: `22 files, 104 tests passed`.
- Production build: `npm --prefix src/underwater_tracking/ui run build` passed.
- Playwright discovery: 10 tests in 3 files, including the live UUV timeline suite.
- A real `python main.py --port 8120 --ui-port 5210` run with the default config passed the initial-state browser test. The API exposed 12 onboard UUVs, no `usvs` field, and no USV text; the Canvas contained non-transparent pixels. Screenshot: `src/underwater_tracking/ui/test-results/uuv-live-default-initial-1440.png`.

## Static checks

- Ruff and `git diff --check` passed for changed Python files.
- Targeted mypy passed for the new summary, source-reader, and event persistence modules.
- A full repository mypy run remains non-green because the baseline contains 214 existing errors across unrelated modules. The report includes errors in legacy NumPy annotations, planning types, runtime types, and the pre-existing `cli.py` shutdown timeout annotation; no new summary-module errors remain.

## Live timeline prerequisite

The strict Playwright test `tests/e2e/uuv-live-timeline.spec.ts` keeps deployment, handoff, recovery, and returned-to-fleet polling enabled when `PLAYWRIGHT_BASE_URL` points at a real `main.py` command center. That test was not run end-to-end in this workspace because `configs/.env` and `UNDERWATER_TRACKING_API_KEY` are unavailable. Without chat credentials the real default process correctly stays paused, so deployment and handoff events cannot occur. The initial-state portion was run against the real process as documented above; no mock API interception was used.
